import react from "react";

import jwt from "jsonwebtoken";

const JWT = () => {
  const post = [
    {
      userName: "bikesh",
      title: "post1"
    }
  ];

  // console.log(post);
  return (
    <>
      <h1>JWT</h1>
    </>
  );
};
export default JWT;
